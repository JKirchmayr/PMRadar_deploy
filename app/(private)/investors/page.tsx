import React from "react";
import Data from "./data/data";

export const metadata = {
  title: "Investor",
  description: "List of investors",
};

const page = () => {
  return <Data />;
};

export default page;
