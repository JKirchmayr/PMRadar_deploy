import React from "react";
import Data from "./data/data";

export const metadata = {
  title: "Companies",
  description: "List of companies",
};

const page = () => {
  return <Data />;
};

export default page;
